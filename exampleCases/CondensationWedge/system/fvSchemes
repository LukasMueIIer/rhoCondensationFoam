// -*- C++ -*-
// File generated by PyFoam - sorry for the ugliness

FoamFile
{
 version 2.0;
 format ascii;
 class dictionary;
 object fvSchemes;
}

ddtSchemes
{
  default Euler;
}

gradSchemes
{ //limiting the R gradients
  grad(R.component(0)) cellLimited leastSquares 1;
  grad(R.component(1)) cellLimited leastSquares 1;
  grad(R.component(2)) cellLimited leastSquares 1;
  grad(R.component(3)) cellLimited leastSquares 1;
  grad(R.component(4)) cellLimited leastSquares 1;
  grad(R.component(5)) cellLimited leastSquares 1;

  default leastSquares;
}

divSchemes
{
  default none;
  scalars Gauss vanLeer ; 	//upwind; 	//linearUpwind default;
  turbulence Gauss vanLeer; 
  energy Gauss vanLeer;

  div(phi,U) Gauss blended  0.8;

  //Energy
  div(phi,e) $energy;
  div(phi,h) $energy;

  //Turbulence
  div(phi,K) $turbulence;
  div(phi,k) $turbulence;

  div(phi,R) $turbulence; //could try upwind
  div(phi,omega) $turbulence;
  div(phi,epsilon) $turbulence;
  div(phi,Ekp) $scalars;
  
  
  //scalars
  div(phi,vapourH2O) $scalars;
  div(Ji,Ii_h) $scalars;

  //special
  div(phi,Yi_h) Gauss multivariateSelection {
        air limitedLinear01             1 ;
        h limitedLinear                 1 ;
        liquidH2O limitedLinear01       1 ;
        vapourH2O limitedLinear01       1 ;
      } ;
  div(((rho*nuEff)*dev2(T(grad(U))))) Gauss linear ;
  div(((rho*(thermo:mu|rho))*dev2(T(grad(U))))) Gauss linear ;
  div((rho*R)) Gauss linear ;
  
}

laplacianSchemes
{
  default Gauss linear corrected;
}

interpolationSchemes
{
  default linear;
}

snGradSchemes
{
  default limited 1.0;
}

wallDist
{
  method meshWave;
  nRequired yes;
} 	// ************************************************************************* //// ************************************************************************* //

